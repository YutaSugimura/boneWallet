export type PrivateKeyFormData = {
  label: string;
  privateKey: string;
};

export type MnemonicFormData = {
  mnemonic: string;
};

export type NetworkFormData = {
  label: string;
  url: string;
};
